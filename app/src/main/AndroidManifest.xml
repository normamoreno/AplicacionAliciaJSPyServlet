<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.utng.jsp_y_servlet">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SeleccionPrincipalActivity" />
        <activity android:name=".FormLoginActivity" />
        <activity android:name=".FormCreateLoginActivity" />
        <activity android:name=".TabActivity" />
        <activity android:name=".MenuAyudaActivity" />
        <activity android:name=".MenuDesarrolladoraActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".VideoIntroduccionActivity" />
        <activity android:name=".ContenidoActivity" />
        <activity android:name=".GraficaMenuActivity" />
        <activity android:name=".TemaActivity" />
        <activity android:name=".JuegoActivity" />
        <activity android:name=".ListaJuegoActivity" />
        <activity android:name=".ListaMenuDosActivity" />
        <activity android:name=".CalculadoraActivity" />
        <activity android:name=".ChronometerActivity" />
        <activity android:name=".GaleriasActivity" />
        <activity android:name=".galerias.GaleryUTNGActivity" />
        <activity android:name=".galerias.GaleryActivity" />
        <activity android:name=".galerias.GaleriaTresActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>
